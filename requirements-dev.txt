# Dev-only dependencies — testing, linting, formatting.
# Install both:  pip install -r requirements-dev.txt
# Layers on top of requirements.txt (which now contains all runtime deps).

-r requirements.txt

# Test framework
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.28.0

# Linting / formatting
ruff>=0.8.0
