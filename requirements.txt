# Production dependencies — used by the core app under app/
# DO NOT add pandas, plotly, matplotlib, or other dev-only libs here.
# Dev tooling (devtools/, scripts/, tests/) goes in requirements-dev.txt.

# Web framework
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.32.0

# Database
sqlalchemy>=2.0.36
alembic>=1.14.0
psycopg2-binary>=2.9.10

# Calculation engine — pure NumPy/SciPy, no pandas
numpy>=2.1.0
scipy>=1.14.0

# Excel I/O
openpyxl>=3.1.5
xlsxwriter>=3.2.0

# Production UI (Streamlit pages live in app/ui/)
streamlit>=1.40.0

# Config
python-dotenv>=1.0.1
pydantic-settings>=2.6.0
